# ServiceAccount for S3 Pod Identity demo.
# Namespace creation is owned by the wrapper recipe / Helm --create-namespace.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: s3-access
  namespace: {{ .Release.Namespace }}
