# Entry point for local k3d deployment (databases + debug pod)
# Uses k3d defaults: traefik ingress, local-path storage
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../../databases
  - ../../base

commonAnnotations:
  targetCluster: local
