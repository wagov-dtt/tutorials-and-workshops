# ArgoCD

This repo packages Kubernetes examples as Helm charts. Deploy them from CI with `helm upgrade --install`, or reconcile the same charts from an orchestration cluster running ArgoCD.

For AWS-managed ArgoCD on EKS, see <https://docs.aws.amazon.com/eks/latest/userguide/argocd.html>.
